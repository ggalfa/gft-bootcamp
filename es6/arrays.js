const arr = ['gleison', 'Amanda', 'Antonia'];

const persons = Array.of('John', 'José', 'Francisco');

console.log(arr);

// Inserir elementos no final array com a função push
arr.push('Cleane');

console.log(arr);

// Remover elementos no final array com a função pop
arr.pop();

console.log(arr);

console.log(persons);

// Inserir elementos no inicio array com a função unshift
persons.unshift('Mary');

console.log(persons);


// Remover elementos no inicio array com a função shift
persons.shift()

console.log(persons);
